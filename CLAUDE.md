# Claude Instructions — up-doc

## Language and Style

- **JavaScript only** — no TypeScript. JSDoc for type hints.
- ESM modules throughout.

## Backlog

Stories live in the backlog directory configured in `.backlog.json`.
Use the `/bl-new`, `/bl-done`, `/bl-sync`, and `/bl-refine` commands to manage stories.
